require 'spec_helper'

describe PublicationDate do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    PublicationDate.create!(@valid_attributes)
  end
end
